# 无广告万年历 App 开发提示词

## 一、项目目标

开发一个**无广告、无内购、离线优先**的中文万年历 App（电子日历），界面简洁清爽，供个人日常使用。请先通读全部需求再动手；如存在技术方案上的取舍，先说明你的选择理由再开始编码。

## 二、技术栈（可按需替换）

- 推荐使用 Flutter（iOS / Android 双端，本地通知能力成熟）；如改用 React Native、微信小程序或 Web PWA，替换本节即可，功能需求保持不变。
- 农历与黄历计算：使用成熟的开源库（如 lunar 系列库），**不要自行实现农历算法**；要求覆盖 1900–2100 年。
- 数据存储：日程与生理期记录使用本地 SQLite；法定节假日数据使用 JSON 配置文件维护，便于每年更新。

## 三、功能需求

### 1. 日历视图（核心）

- 月视图为 App 首页：每个日期同时显示公历与农历，节气、节日直接标注；周末与法定节假日标红，调休补班日标注"班"字角标。
- 支持月视图 / 周视图 / 年视图切换，左右滑动翻页，顶部可快速选择年月。
- 点击任意日期进入"当日详情"页：完整公历、农历、干支（年月日）、生肖、节气、节日、宜忌，以及当天的日程与生理期状态。

### 2. 农历与传统信息

- 农历日期（如"正月初一"）、天干地支纪年/纪月/纪日、生肖。
- 二十四节气的标注与当日节气提示。
- 节日显示：传统节日按农历/节气（春节、元宵、清明、端午、七夕、中元、中秋、重阳、腊八、除夕等），现代节日按公历（元旦、妇女节、劳动节、儿童节、国庆节等）。

### 3. 国家法定节假日与调休

- 展示国务院公布的当年法定节假日放假安排与调休补班安排；放假日在日历中标红，补班日标注"班"。
- 节假日数据以 JSON 文件维护（内置 2025、2026 年完整数据），支持在设置页导入/更新；无数据年份优雅降级（仅周末标红，不误标法定假日）。
- 首页提供"假期倒计时"：距下一个法定假期还有 X 天。

### 4. 每日宜忌（黄历）

- 当日详情页展示：宜、忌事项列表，吉神凶煞，冲煞。
- 由 lunar 库按传统黄历规则推算，页面显著位置注明"民俗内容，仅供参考"。

### 5. 生理期记录（隐私优先）

- 一键记录每次经期的开始与结束日期，支持补记和修改历史记录。
- 设置页可调整平均周期长度（默认 28 天）与平均经期天数（默认 5 天）。
- 基于历史记录预测下次经期、排卵日、易孕期、安全期，并在日历上以不同颜色标注；**预测日期与实际记录必须在视觉上可区分**（如预测用空心/浅色）。
- 隐私要求：数据仅存本地，不上传；提供应用锁（PIN 或生物识别，视平台能力而定）。
- 统计页：近 6 个月周期长度图表、平均周期、上次经期开始日期；周期明显不规律时给出温和提示建议就医。

### 6. 日程与提醒

- 日程支持：标题、日期、开始/结束时间、备注、分类标签（工作 / 生活 / 纪念日等，颜色区分）、重复规则（不重复 / 每天 / 每周 / 每月 / 每年 / 自定义）。
- 到点通过系统本地通知提醒，可选提前量（准点 / 5 分钟 / 1 小时 / 1 天）；生理期 predicted 开始日也可推送温和提醒。
- 月视图在有日程的日期下方显示小圆点；当日详情页列出全部日程，支持完成勾选与删除。
- 节假日与农历节日支持"一键转为日程"，方便自定义提醒。

### 7. 其他

- "回到今天"一键按钮；支持跳转到任意日期（含过去与未来，如 2030-01-01）。
- 深色模式跟随系统。

## 四、非功能需求

- 无任何广告、开屏页、推荐位、内购与第三方统计 SDK。
- 核心功能全部离线可用；不要求注册账号；不采集任何个人数据。
- 全中文界面，遵循对应平台的设计规范（Material Design / 人机界面指南）。
- 性能：月视图滑动流畅不掉帧，冷启动不超过 2 秒。

## 五、交付物

1. 可直接构建运行的完整项目源码。
2. README：构建运行步骤、节假日数据的更新方法、技术选型说明。
3. 节假日 JSON 数据文件（至少覆盖 2025–2026 年）。
4. 关键模块单元测试：农历转换正确性、节假日/调休判断、生理期预测算法。

## 六、工作方式

请分阶段实施，每阶段完成后暂停，交我验收后再进入下一阶段：

1. 日历核心：月视图 + 农历 + 节日显示
2. 法定节假日与调休 + 每日宜忌
3. 日程管理与本地提醒
4. 生理期记录、预测与隐私保护
5. 打磨：深色模式、应用锁、统计图表、性能优化
