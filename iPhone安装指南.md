# iPhone 安装指南（免付费、免 Mac、免开发者账号）

## 先说清楚：iPhone 为什么没有"安装包"

安卓手机可以侧载 APK，但 **iOS 不允许**：任何装上 iPhone 的应用都必须经过苹果的签名体系（Xcode 打包，需要 Mac 电脑，个人免费签名 7 天过期，正式分发需 688 元/年的开发者账号）。在 Windows 电脑上无法生成能装进 iPhone 的安装包——这不是工具问题，是苹果的平台规则。

但苹果官方为这类工具应用准备了一条正路：**PWA（添加到主屏幕）**。装好后的效果：

- ✅ 桌面独立图标（红色日历图标），点开全屏运行，没有浏览器地址栏
- ✅ **完全离线使用**（数据、日历、宜忌、日程、生理期全部在本机）
- ✅ 无广告、无账号、数据不上传（和现在完全一致）
- ✅ 永不过期，不会被苹果"吊销签名"
- ✅ 免费，5 分钟装好

唯一的要求：**第一次安装需要通过一个 HTTPS 网址**（苹果规定），装好之后就不依赖网络了。

---

## 安装步骤（约 5 分钟）

### 第 1 步：把万年历发布到一个免费 HTTPS 网址

解压 `万年历-iPhone部署包.zip`，把里面的 `wanlianli` 文件夹**整个**上传到任意静态网站托管服务。推荐 GitHub Pages（免费、稳定、步骤如下）：

1. 电脑浏览器打开 <https://github.com> → Sign up 注册一个免费账号（只需邮箱）
2. 登录后点右上角 **＋ → New repository**，名字随意（如 `calendar`），选 **Public**，点 Create repository
3. 在新仓库页面点 **uploading an existing file**，把 `wanlianli` 文件夹里的**全部 21 个文件**（保持文件夹结构）拖进去，点 **Commit changes**
   - 如果不熟悉上传文件夹，也可以在仓库页面点 **Add file → Upload files**，或安装 GitHub Desktop 桌面软件直接拖文件夹
4. 仓库页点 **Settings → Pages**（左侧栏），Source 选 `Deploy from a branch`，Branch 选 `main` / `/ (root)`，点 **Save**
5. 等 1–2 分钟，页面顶部会显示网址，形如 `https://你的用户名.github.io/calendar/`
6. 用**电脑浏览器**先打开这个网址，确认万年历能正常显示

> 不想用 GitHub？任何支持静态托管的 HTTPS 服务都可以（如 Cloudflare Pages、Netlify、腾讯云 Webify 等），只要把 `wanlianli` 文件夹作为网站根目录上传即可，后续步骤相同。

### 第 2 步：在 iPhone 上添加到主屏幕

1. iPhone 打开 **Safari**（必须是 Safari，微信/Chrome 里不行），访问上面的网址
2. 点屏幕底部中央的 **分享按钮**（方框加向上箭头）
3. 向下滑，点 **"添加到主屏幕"** → 确认名称为"万年历" → 点右上角 **添加**
4. 回到桌面，你会看到红色的万年历图标——**从图标打开它，以后都用这个图标进入**

### 第 3 步：验证离线（单机）可用

1. 从主屏幕图标打开万年历，随意浏览几个页面（让缓存完整建立）
2. 开启**飞行模式**，再从图标打开——一切功能正常，即为安装成功
3. 建议顺手完成两件事：
   - **生理期标签页** → 周期设置：调整你的周期参数、开启经期提醒
   - **设置页** → 通知：点"开启通知"（iOS 16.4 及以上在添加到主屏幕后支持通知）

---

## 装好之后

| 事项 | 说明 |
|---|---|
| 数据存在哪 | 只在你手机的本机存储里，不上传任何服务器 |
| 换手机 / 换机迁移 | 旧手机：设置 → 数据备份 → 导出备份（得到一个 JSON 文件，通过隔空投送/微信传给自己）；新手机：装好后 设置 → 导入备份 |
| 更新次年节假日 | 每年 11 月国务院公布新安排后：**设置 → 点"🔄 更新假期"联网自动获取**（最方便）；也可以导入新 JSON 数据文件，或重新部署更新包 |
| 应用更新 | 修改电脑上的文件后重新上传托管，手机上重新打开即自动更新（数据不受影响） |
| 删除应用 | 长按图标删除即彻底删除（连同本机数据）；删除前建议先导出备份 |

## 已知限制（iPhone 平台）

- **通知**：iOS 16.4+ 且已添加到主屏幕时可用；应用完全退出期间不弹通知（无后台推送，符合"离线、不采集数据"的设计）
- **首次安装**必须有网络和 HTTPS 网址，之后离线
- 如果你将来真想要**原生 iOS App**（上架 App Store 或 TestFlight），需要 Mac 电脑 + 苹果开发者账号（688 元/年），届时可以把这套代码用 Capacitor 封装，界面和功能不变

## 常见问题

**Q：添加到主屏幕时找不到这个选项？**
A：必须用 Safari 打开网址，且要先把网址完整加载出来；微信里打开的页面没有此选项。

**Q：图标打开后顶部有浏览器地址栏？**
A：说明不是从主屏幕图标进入的，或第 1 步的文件没有传全（缺 `manifest.webmanifest`）。从桌面图标进入即为全屏。

**Q：飞行模式下打不开？**
A：先在有网时从图标打开一次、浏览全部三个标签页让缓存完成；仍不行说明托管未启用 HTTPS，检查网址是否为 `https://` 开头。
