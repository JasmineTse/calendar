# 安卓手机安装指南（免费、免签名）

安卓对 PWA 的支持比 iPhone 更完整，安装更简单，**通知、离线缓存全部可用**，且永不过期。

## 安装步骤（约 2 分钟）

1. 先按 [iPhone安装指南.md](iPhone安装指南.md) 的"第 1 步"，把 `万年历-iPhone部署包.zip` 里的 `wanlianli` 文件夹发布到一个 HTTPS 网址（**安卓和苹果共用同一个网址即可**，一次部署两边都能装）
2. 安卓手机用 **Chrome**（或其他 Chromium 内核浏览器，如 Edge、夸克、Via）打开该网址
3. 点浏览器右上角 **⋮ 菜单** → **"添加到主屏幕"** 或 **"安装应用"**
4. 确认安装，桌面出现红色万年历图标——从图标打开即为全屏独立应用

> 厂商自带浏览器的支持参差不齐：小米/华为/OPPO 等自带浏览器一般也能"添加书签到桌面"，但离线和通知能力可能不完整，建议优先用 Chrome。

## 与 iPhone 版的差异（安卓更强）

| 能力 | 安卓 | iPhone |
|---|---|---|
| 离线使用 | ✅ 完整 | ✅ 完整 |
| 日程/经期到点通知 | ✅ 完整 | ⚠️ 需 iOS 16.4+，且应用运行期间才弹 |
| 安装门槛 | 无 | 需 HTTPS 托管 |
| 有效期 | 永久 | 永久 |

## 日常使用

- **数据**：只存本机；换手机用 设置 → 导出备份 / 导入备份 迁移
- **更新次年节假日**：设置 →"🔄 更新假期"联网获取（安卓上通知权限不受限，提醒体验更完整）
- **卸载**：长按图标卸载即可；先导出备份再卸载更稳妥

## 想要真正的 APK 安装包？

PWA 方式在安卓上已经是"官方推荐的原生级体验"。如果你还是想要一个可以离线分发的 `.apk` 文件（比如发给不便上网的家人），可以用 Capacitor 把这套代码打包成 APK——需要在这台电脑上下载约 2GB 的安卓构建工具链，耗时较长；需要的话直接说，我来配置打包。
